# DrPangloss Detailed Review and Implementation Log

Date: 2026-03-03

## Goals
1. Improve reliability and maintainability with low-risk changes first.
2. Add regression tests for known correctness issues.
3. Keep architecture options open for upcoming composable models and linear observables (kernel phase, DISCO).

## Repository Audit Highlights

### Code Quality
- `cp_flag` logic currently mislabels absolute phase as closure phase in `OIData`.
- Internal phase units are inconsistent between absolute phase and closure phase paths.
- `OIData.__init__` catches broad exceptions, masking data-format errors.

### Testing
- Existing tests are concentrated in a single file and include plotting-heavy workflows.
- Limited direct regression coverage of known bugs.

### Docs and Tooling
- README/docs are sparse in key places.
- Contribution and CI setup contain stale elements.

## Low-Risk Implementation Plan (Current Batch)
- Create persistent notes files (this file + root summary).
- Apply docs/metadata consistency fixes.
- Modernize CI workflow actions and trigger policy.
- Add targeted regression tests around `OIData` and phase handling.
- Apply correctness-only bug fixes in `models.py` with compatibility in mind.
- Validate via test run and record outcomes.

## Issue Mapping Snapshot
- #25 `cp_flag` bug -> low-risk correctness fix in current batch.
- #28 units inconsistency -> low-risk correctness fix in current batch.
- #26 likelihood semantics -> partially addressed now (documentation/tests), full statistical API redesign deferred.
- #27 optimization outputs consistency -> deferred (grid API redesign tier).
- #20/#22 composable and expanded models -> deferred to major architecture phase.

## Change Log
- 2026-03-03: Initialized detailed review log and execution tracking.
- 2026-03-03: Updated package metadata license path in `pyproject.toml` (`LICENSE`).
- 2026-03-03: Refreshed repository-facing docs in `README.md`, `docs/index.md`, and `CONTRIBUTING.md` to remove stale placeholder and path references.
- 2026-03-03: Corrected MkDocs handler setup path from `dLux` to `src` in `mkdocs.yml`.
- 2026-03-03: Modernized GitHub workflows to current action versions and pull-request validation paths.
- 2026-03-03: Fixed low-risk `OIData` correctness issues in `src/drpangloss/models.py`:
	- `cp_flag` is now `False` for absolute phase (`OI_PHI`) data.
	- `to_phases()` now returns degrees for absolute phases to match closure-phase convention.
	- `closure_phases()` degree wrapping path was made internally consistent.
	- dict-based initialization now uses explicit branch logic and safer defaults.
- 2026-03-03: Added focused regression tests in `tests/test_regressions.py` for phase units and `cp_flag` inference behavior.
- 2026-03-03: Created a `uv`-managed Python 3.11 environment (`.venv`) and migrated local test execution to `uv run`.
- 2026-03-03: Added `.python-version` pin (`3.11`) and updated `README.md` + `CONTRIBUTING.md` with `uv` setup commands.
- 2026-03-03: Regression validation in the new environment: `3 passed` for `tests/test_regressions.py`.
- 2026-03-03: Addressed a Python 3.11 compatibility break in `absil_limits` path caused by `numpyro`/`tensorflow_probability` import behavior inside `gammaincinv`.
- 2026-03-03: Implemented a careful fix in `chi2ppf`:
	- For `df=1` (current `nsigma` use), use a JAX-native closed form via normal quantile, preserving differentiability and speed.
	- Retain `gammaincinv` fallback for `df != 1`.
- 2026-03-03: Added regression test `test_chi2ppf_df1_returns_finite_values`.
- 2026-03-03: Full current test execution under `uv`/Python 3.11: `15 passed`.
- 2026-03-03: Added pytest configuration in `pyproject.toml` (`testpaths`, `python_files`) so `pytest` discovers both new-style tests and the legacy `tests/tests.py` module.
- 2026-03-03: Modernized `.github/workflows/tests.yml` and `.github/workflows/documentation.yml` to use `uv`-managed Python 3.11 environments for reproducible CI runs.
- 2026-03-03: Refactored tests into focused modules:
	- `tests/_test_data.py` shared setup and fixtures
	- `tests/test_models_core.py` model/core likelihood tests
	- `tests/test_grid_and_limits.py` grid/optimization/limit tests
	- removed legacy `tests/tests.py`
- 2026-03-03: Added `tests/__init__.py` to support intra-test imports after split.
- 2026-03-03: Performed repository-wide docstring quality pass and fixed missing docstrings in active and legacy modules (`models.py`, `plotting.py`, `oifits_implaneia.py`, `savefits.py`, `models_old.py`).
- 2026-03-03: Added `docs/api.md` and updated `mkdocs.yml` nav + mkdocstrings configuration so API docs propagate from source docstrings.
- 2026-03-03: Fixed malformed NumPy sections in selected docstrings that were causing strict docs parser warnings (`models.py`, `grid_fit.py`).
- 2026-03-03: Validation under `uv`/Python 3.11:
	- `pytest` => 15 passed
	- `mkdocs build --strict` => pass
- 2026-03-03: Added notebook-adapted documentation pages that avoid large data requirements by using compact synthetic observables:
	- `binary_recovery_grid_hmc.md` (binary recovery with grid search + HMC/NUTS)
	- `contrast_limits_ruffio.md` (Ruffio contrast-limit workflow)
- 2026-03-03: Added both pages to MkDocs navigation and validated docs build in strict mode.
- 2026-03-03: Added a full synthetic-data file workflow for docs users:
	- Generate synthetic binary observables.
	- Save to OIFITS with DrPangloss OIFITS writer and headers.
	- Reload through both dictionary loader and `OIData` path.
	- Perform brief grid + HMC recovery analysis.
	- Files: `docs/synthetic_data_file_roundtrip.md`, `docs/examples/synthetic_binary_workflow.py`.
- 2026-03-03: Added synthetic-docs CI gate:
	- Test: `tests/test_docs_synthetic_recovery.py`
	- Fails on any runtime error in generation/save/load/recovery pipeline.
	- Fails if recovered `(dra, ddec, flux)` from HMC deviates by more than `2σ` from simulated truth.
- 2026-03-03: Updated documentation workflow to run synthetic-docs validation test.
- 2026-03-03: Cleanup pass on docs/test warnings:
	- Moved executable synthetic helper from `docs/examples` to `examples/synthetic_binary_workflow.py` and updated docs/tests references.
	- Constrained `mkdocs-jupyter` include scope to notebooks.
	- Replaced deprecated Simbad field `sptype` with `sp_type`.
	- Escaped LaTeX-style label strings in `oifits_implaneia.py` to remove invalid-escape deprecation warnings.
	- Added pytest filter for external Optax-triggered JAX config deprecation (`jax_pmap_shmap_merge`) to keep CI signal focused.
- 2026-03-03: Validation after cleanup:
	- `pytest tests/test_docs_synthetic_recovery.py` => pass
	- `pytest` => 16 passed
	- `mkdocs build --strict` => pass

## Next Complex Milestones (Deferred)
1. Observable schema abstraction.
2. Kernel-phase and DISCO operators with optional covariance weighting.
3. Grid parameter-axis metadata and contract hardening.
4. Composable source models and model registry.
