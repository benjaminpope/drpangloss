repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    # Keep in sync with [tool.ruff] required-version in pyproject.toml.
    rev: v0.11.0
    hooks:
      - id: ruff
        args: [--fix]
      - id: ruff-format
        exclude: ^notebooks/.*\.ipynb$
